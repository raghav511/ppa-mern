import "./Header.css";

const Header = () => {
    return (
        <header>
            <a>Home</a>
            <a>Search</a>
            <a>Help</a>
        </header>
    );
};

export default Header;
